import { SchedulePage } from './schedule-page'

export const metadata = {
  title: 'Scheduling — appkit',
  description: 'Critical-path Gantt, work-breakdown outline, calendars, baselines, and resource levelling.',
}

export default function SchedulingDemoPage() {
  return <SchedulePage />
}
