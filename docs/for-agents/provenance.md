# Provenance audit

appkit has three kinds of code. Do not blur them:

- **Faithful extraction** — ported from a named production sibling source;
  app coupling is replaced by props/providers, but behavior and visual grammar
  stay aligned.
- **Generalized production pattern** — a real sibling flow expressed as a
  reusable primitive or framework-neutral package.
- **appkit-original** — no matching production implementation existed. These
  components must not be presented as extracted or sibling-identical.

This inventory was audited against reference application A commit
`8d6ace86d3faf92381f2dbfe9ed0d14d937d7beb` and reference application B commit
`2bc3d36ae435b6bb7072a9c990b835bbce47fd0e` on 2026-07-20.

The `@braedonsaunders/create-appkit` scaffold and package build/release toolchain are
**appkit-original infrastructure**. They compose and distribute the audited
surfaces below; they are not presented as extracted product features.

## Application shell and navigation

| appkit surface | classification | production source |
|---|---|---|
| `AppShell` | faithful generalized extraction | reference A `web/components/app-shell.tsx`; reference B `apps/web/src/components/app-shell.tsx` |
| `TopNav` | faithful generalized extraction | reference A `web/components/top-nav.tsx` and `web/lib/top-nav-overflow.ts` |
| `AppSidebar` | faithful generalized extraction | both apps' `components/app-sidebar.tsx` |
| `SidebarNav` | compatible superset | reference A workspace/subgroup implementation plus the reference B grouped-list contract |
| `NavigationConfigEditor` / tenant navigation config helpers | faithful generalized extraction | reference B `apps/web/src/app/(app)/admin/navigation/_editor.tsx` and `apps/web/src/lib/nav/registry.ts`; app routes, permissions, and persistence are injected |
| `MobileTabBar` / mobile selection | faithful generalized extraction | both apps' `components/mobile-tab-bar.tsx`; reference A `web/lib/mobile-nav.ts` |
| mobile drawer inside `AppShell` | generalized production pattern | both apps' `components/mobile-nav-toggle.tsx`, implemented with appkit's extracted `Drawer` |
| `AccountMenu` | compatible generalized extraction | reference A `web/components/account-menu.tsx` launcher visual grammar; choices/persistence are controlled props so any app can supply its policy |
| `GlobalSearch` | generalized production pattern | both apps' `components/global-search.tsx`; domain querying and route selection are injected |
| `NotificationsBell` | compatible superset | reference A inbox list plus the reference B controlled unread/menu pattern; items and mutations are injected |
| theme/navigation/link providers | faithful generalized extraction | both apps' `theme-provider.tsx`, `theme-toggle.tsx`, `app-link-provider.tsx`, and `navigation-provider.tsx`; sibling UI `link-context.tsx` was byte-identical |

The flat-link top navigation introduced in appkit commit `21b98ad` was invented
and was not sibling-compatible. It was removed rather than retained as a second
navigation system.

## UI package

Faithful or generalized extractions:

- Base primitives: `Alert`, `Badge`, `Button`, `Card`, `ContextMenu`, `Drawer`,
  `DocumentTitle`, `DetailHeader`, `EmptyState`, `Input`, `Label`, `MultiSelect`,
  `PageHeader`, `Popover`, `SearchSelect`,
  `Separator`, `Skeleton`, `Table`, `Textarea`, and utilities come from the
  siblings' `packages/ui/src` implementations and are tokenized here.
- `PageContainer`, list/detail/wizard layouts come from both apps'
  `components/page-layout.tsx`.
- `LineGrid` comes from reference A `web/components/line-grid.tsx`.
- `AdminHub`, `SettingsShell`, `SettingsNav`, `SettingsSection`, and
  `SettingsRow` come from the reference A admin hub and setup shell. `AdminHub`'s
  detailed capability-card layout is a generalized extension of that same card
  grammar for package inventories; the compact settings layout remains the
  faithful default.
- `RecordList` and URL-driven search/filter/sort/pagination are generalized from
  reference A production list pages and navigation helpers. URL-backed search
  preserves the latest local edit across overlapping streamed navigations, so
  an older response cannot replace newer keystrokes.
- `PagedTable` is copied from reference A `web/components/paged-table.tsx` with
  localized copy moved to props and raw palette values tokenized. Its bounded
  search, clamped pages, aligned cells, row classes, and source caller signature
  are retained. Client-side sortable columns (`sortValue`/`defaultSort`), row
  activation (`onRowClick`), and the searched-to-empty row are additive
  generalizations that reuse `RecordList`'s existing header and row grammar; the
  source's own props are unchanged. `SearchSelectFilter` is the corresponding long-option URL
  filter from `filter-bar.tsx`, routed through AppKit's injectable list-nav
  provider. `SubtabNav` is the source `documents/DrawerTabs.tsx` detail/drawer
  grammar with optional counts and disabled states as additive generalizations.
- `Select` is the reference B native-`<select>`-API implementation from
  `packages/ui/src/select.tsx` and `select-options.ts`, copied
  at source parity with its upstream tests. It preserves `<option>`/
  `<optgroup>`, form submission, required validation, refs, and genuine change
  events while presenting `SearchSelect`. Options-array consumers use
  `SearchSelect` explicitly; AppKit no longer substitutes a different contract
  under the `Select` name.
- `AnimatedNumber` is a token-timed extraction of the byte-identical sibling UI
  primitive. `Sparkline` is the shared sibling implementation with i18n replaced
  by labels and every raw chart color replaced by semantic tokens.

appkit-original UI with no direct sibling component source:

- `ApiReference`, `ApiEndpoint`, `MethodBadge`, and `CodeBlock`.
- `Avatar`, `Checkbox`, `Dialog`, `Progress`, `Spinner`, `Switch`, `Tabs`, and
  `Tooltip`.
- The Sonner-compatible `toast`/`Toaster` implementation. Its API mirrors what
  the siblings consume, but the implementation is appkit's.
- `PageTransition` and the `appkit-page-transition` browser-native route motion;
  neither sibling had a routed-page transition implementation to extract.
- The standalone component-gallery composition and appkit brand artwork.

These originals may be useful, but they are not evidence of reference-app
parity until a consuming app adopts them or they are explicitly reconciled with
a new sibling source.

## Platform packages

| package | classification | production lineage |
|---|---|---|
| `@braedonsaunders/create-appkit` | appkit-original infrastructure | Public scaffold CLI that composes the audited package surfaces without inventing product domain screens. |
| `@braedonsaunders/appkit-tokens` | appkit-original abstraction | semantic-token layer built around the siblings' real light/dark palette and UI values |
| `@braedonsaunders/appkit-ui` | audited mixed extraction | Every exported UI surface is classified in the UI section above; extracted primitives retain source behavior while AppKit-original primitives are named explicitly rather than presented as source parity. |
| `@braedonsaunders/appkit-analytics` | substantial generalized extraction | The production catalogue/compiler and formula engine are preserved beside schema-discovered semantic entities, typed flat/pivot results, dense pivot construction, fifteen visualization definitions with renderability checks, library-neutral chart specs, tokenized conditional formatting, and fail-closed persisted-query validation. Tenant schema introspection and custom-field loading remain explicit application adapters because their data models are application-owned. |
| `@braedonsaunders/appkit-dashboard` | compatible production superset | The responsive production grid, searchable library, quick-action preservation/validation, user-role-default layout resolution, wildcard/tier access policy, source card-authoring flow, fifteen result renderers, and typed pivot/heatmap renderer are present. `DashboardStudio` adds a controlled metadata, autosave, publish, pin, and delete shell around those source behaviors. No non-existent source panels are claimed. |
| `@braedonsaunders/appkit-reports` | substantial faithful generalized extraction — BeaconHS cutover in progress | The paper/table exports use one application-neutral public API; migration-only component aliases are intentionally excluded. The paper preview preserves the source document composition and now applies the canonical page margin, density, orientation, paper width, and summary visibility from the same layout used by document output. The production statement viewer behavior is present in full: grouped headings, hierarchy, accounting currency, scale, variance percentages, totals, collapse/expand, account navigation, typed drill targets, and the paged supporting-row drawer. The compact toolbar restores every source control through application-owned state. Stored queries use the shared production `ReportCustomQuery`/`op`/`fn` contract, and packed-consumer tests compile both source catalogue families without translation. The one-third/two-thirds studio includes grouped searchable catalogues, catalogue-derived templates, ordered/renamable and searchable columns, recursive filters, three-level synchronized sorting, page setup, debounced live preview/autosave, manual run/save, destructive delete, paper preview, and export. Autosave only advances its persisted baseline after a successful write, locks authoring controls while persistence is in flight, accepts server-normalized controlled values, and exposes host-owned post-save and post-delete navigation callbacks; confirmation and durable delete semantics remain application adapters. Catalogues may retain hidden columns for trusted base scoping without exposing technical identifiers to custom queries. Scheduled reports use that same recursive compiler-native filter tree against a host-authorized entity; raw JSON and product-specific rolling-day controls are not a second query contract. The scheduled-report extraction preserves the production `definitionId`/`active` contract, full cadence and nth-weekday grammar, timezone and date bounds, member and external recipients, bounded filters, email copy, shared create/update parser, searchable register, pause/resume, run-now, execution history, failure detail, and artifact transport seam. BeaconHS now consumes the studio, viewer, schedules, durable-run contract, and compiler through its domain catalogue and persistence adapters; full browser and deployment gates remain pending before this can be called a completed cutover. Product catalogues, SQL loaders, persistence, recipient lookup, native record drawers, and export/delivery routes remain injected because they are application-owned. |
| `@braedonsaunders/appkit-ai` | substantial generalized extraction | The bounded agent loop and thread UI sit beside the production analysis, extraction, document-chat, vision, writing, digest, model-policy, prompt, and provider-client helpers. Credentials and application tool registries are injected; no provider secret or product prompt catalogue is bundled. |
| `@braedonsaunders/appkit-db` | generalized extraction | Reference A DB executor/schema conventions plus the reference B Postgres RLS engine |
| `@braedonsaunders/appkit-tenant` | faithful generalized extraction — adapter-ready; reference cutover pending | Reference B's request context, timezone and locale policy, active-role resolution, impersonation guard, template/payload access helpers, scope ordering, wildcard/read-tier RBAC, concrete deny behavior, and exact `resolveMembershipAccess(tx, membershipId, activeRoleId?)` call shape are preserved. `createMembershipAccessResolver` binds an application catalogue once and fails closed if a wildcard carve-out is underconfigured. `createTenantContextFactory` retains the source context-constructor call shape over an application-owned RLS runtime, while `RequestContext<TExtension>` carries product-owned identity or terminology without putting it in AppKit. Tenant permission evaluation is dependency-correct and standalone; optional IAM now consumes/re-exports it instead of the foundation depending on IAM. Package tests cover source behavior and the packed consumer compiles the unchanged resolver call plus a typed application extension. A reference application import cutover remains required before a completed drop-in claim. |
| `@braedonsaunders/appkit-iam` | faithful generalized extraction — packed reference-shaped consumer proven; application cutover pending | Reference B `admin/roles` list/detail/actions, permission matrix, scoped role-member manager, bulk add/replace/remove, `admin/users` lifecycle/scope/override model, invitation resend generation, and IAM schema are combined with reference A `RoleEditor`, paged role/user registers, source filter counts, role list/API protections, permission deny semantics, destructive confirmations, user actions, and the complete audit list/detail drawer. AppKit retains service-backed filtering/sorting/paging and stable facets, responsive member cards, key/email columns, actor-aware built-in/current-user/super-admin protections, runtime permission/scope validation, atomic duplicate/delete/bulk writes, invitation persistence and rotation, scoped assignment upsert, per-user grant/deny overrides, record activity, before/after audit, application lifecycle hooks, typed UI/action extensions, and authenticated HTTP and memory adapters. The Drizzle adapter compiles against both production driver families (`node-postgres` and `postgres-js`), and the packed consumer exercises the source-shaped bulk/resend/record-audit calls and React extension seams. Browser verification covers role filtering/detail/protected permissions/bulk UI, member facets/invitation resend, destructive confirmation, and audit filtering/detail with a clean console. Product permission catalogues, hierarchy choices, invitation credential delivery, route authorization, and product-owned identity panels remain typed application inputs. An adjacent production application still needs to replace its imports before a completed application-cutover claim. |
| `@braedonsaunders/appkit-superadmin` | generalized production pattern | Bidwright's installation-wide account, credential, activation, super-admin, and live-session administration at commit `76cf58d203001d2111eaf3fb67cdf3b8a380c2df` is expressed as service, memory, Drizzle, and React boundaries. Authentication middleware, password hashing, authorization, and routing remain application-owned. |
| `@braedonsaunders/appkit-auth` | substantial faithful generalized extraction — reference-consumer cutover pending | Reference B's Better Auth server/client runtime, password reset lifecycle, hashed magic links, membership-targeted invitation grants, durable core schema, lazy singleton, and React sign-in/reset forms are preserved. Reference A's active-user field and UUID/plural-table schema are retained through Better Auth's explicit model/field mappings. Email transport, brand rendering, tenant cookie selection, post-acceptance domain effects, and route authorization are injected. Direct reference-application compile cutovers remain required before a drop-in claim. |
| `@braedonsaunders/appkit-events` | substantial generalized extraction | Structured audit, transactional outbox writes, recipient resolution, lease-based claiming, durable retry transitions, effects ledger, HMAC internal authentication, and a bounded relay are present. Concrete transport delivery is an injected handler so the package remains vendor-neutral. |
| `@braedonsaunders/appkit-notifications` | faithful generalized production UI and substantial notification runtime extraction | Reference B `apps/web/src/app/(app)/notifications/_outlook-inbox.tsx`, `actions.ts`, and `loading.tsx` supply the complete three-pane inbox. Its `preferences/_form.tsx`, `_push-toggle.tsx`, `push-actions.ts`, `admin/notifications/_form.tsx`, `_actions.ts`, `_schedule.ts`, `packages/db/src/schema/{notifications,tenant-settings}.ts`, worker digest scanner, notify/push workers, and queue profiles supply the newly extracted preference matrix, browser/OS push enrollment, tenant routing cockpit, schedule grammar, policy fields, normalization, digest/push lifecycle, and job contracts. AppKit preserves transport readiness, digest/quiet hours, scan enablement/cron/timezone, roles, people, groups, channels, escalation, dirty saves, missing-cell-enabled semantics, iOS guidance, deterministic 250-recipient batching, retry/backoff, and retention. Framework routes, permission gates, category and recipient catalogues, RLS handles, credentials, provider transports, audit projection, and domain to-do queries are application adapters. The generalized dispatcher and Drizzle stores retain idempotent inbox writes and stable delivery keys; a direct reference worker import cutover remains unproven. |
| `@braedonsaunders/appkit-mcp` | generalized extraction | Reference A's production MCP endpoint, expressed as a framework-neutral toolkit: gated tool-catalogue registration (invisible tools are never registered), the stateless streamable-HTTP exchange with guaranteed teardown, DNS-rebinding host validation and origin allowlisting, the success/failure result contract with an injectable error mapper that collapses unmapped errors before they reach the model, static ground-rule resources, and per-call audit reporting. Application tool catalogues, authentication, permission gates, and persistence remain injected adapters. |
| `@braedonsaunders/appkit-api` | generalized extraction | Reference B `apps/web/src/lib/api` layer, made framework-neutral; OpenAPI description helpers are appkit-original extensions |
| `@braedonsaunders/appkit-sandbox` | faithful generalized execution-kernel extraction | Reference A `engine/src/apps-runtime.ts` and `engine/src/scripting.ts`; the byte-level duplicated QuickJS memory/stack/deadline/async-host mechanics are unified behind generic host capabilities and structured faults. |
| `@braedonsaunders/appkit-endpoints` | faithful generalized extraction | Reference A `engine/src/apps-runtime.ts`; the source-shaped `runAppEndpoint` export is also available from `@braedonsaunders/appkit-apps/runtime`, while application-specific writes are injected host functions. |
| `@braedonsaunders/appkit-scripts` | substantial faithful generalized extraction — consuming-app route cutover pending | Reference A `engine/src/scripting.ts`, `web/lib/client-scripts.ts`, script worker/queue contracts, `schema/src/extension.ts`, and `web/app/(app)/admin/scripts/ScriptDrawer.tsx`. The source CodeMirror JavaScript editor, line numbers/folding/completion, trigger-aware starter source, general/code/runs/log drawer flow, run selection, and log viewer are preserved behind controlled callbacks. Event ordering, aborts, allowed mutations, read-only data seams, endpoint/scheduled/bulk/client runtimes, cron, audit history, Drizzle, React authoring, and queue-neutral jobs are present. `/bound` has a tested positional cutover contract that preserves an application's authored global, native input context, host values, and adapters without hardcoding them. Route authorization and application query/write adapters remain host-owned. |
| `@braedonsaunders/appkit-apps` | substantial faithful generalized extraction — consuming-app route cutover pending | Reference A `engine/src/apps-runtime.ts`, `schema/src/apps.ts`, `web/lib/apps/{manifest,zip,objects,bridge,store}.ts`, installed App frame, marketplace/library, API lifecycle, and `web/app/(app)/admin/apps/AppDrawer.tsx`. The source `xl` editor drawer and inset, rounded file workbench are preserved with the same 15rem collapsible tree, file-type icons, CodeMirror JavaScript/HTML/CSS/JSON modes, line numbers/folding/completion, binary inspector, create/upload/delete/save controls, overview, and run inspection; AppKit additionally retains its governed live preview. Manifest/ZIP validation, immutable versions, object provisioning transaction seam, storage, capability intersection, QuickJS endpoints, opaque-origin iframe/CSP/SDK, publishing, memory/Drizzle persistence, and React authoring are present. `/service` and `createAppEndpointRuntime` provide tested source-shaped lifecycle and endpoint cutover seams with application-supplied global, scope mapping, permissions, records, and governed writes. Authentication, routes, and domain object schemas remain injected. |
| `@braedonsaunders/appkit-crypto` | generalized application-agnostic runtime | The AppKit root uses `APPKIT_SECRET` and `appkit.secret.v1`; `createSealer(secret, { hkdfInfo })` covers the shared object envelope, while `createContextualSealer` preserves established versioned compact payloads with caller-owned tenant salt and record AAD. This lets applications own migration compatibility without shipping application-named entries. |
| `@braedonsaunders/appkit-process-sandbox` | faithful generalized extraction | Bidwright `apps/api/src/services/agent-host/bubblewrapped.ts` supplies the production bubblewrap namespace, mount, masking, environment-clearing, and workspace-binding policy. AppKit generalizes product paths, commands, credentials, and egress variables into typed inputs, adds a pure auditable plan builder, supports either private procfs or a process-metadata-free synthetic self-executable for native-runtime compatibility, and makes unsupported hosts and missing bind sources fail closed. AppKit-original additions: `network: 'none' \| 'host'` namespace policy (defaulting to `'host'` so the extracted behavior is preserved on upgrade) and `prlimit`-backed kernel resource ceilings that fail closed when util-linux is absent, both surfaced in the auditable plan and probed by `verifyProcessSandbox`. Tenant identity resolution, egress filtering beyond namespace isolation, and desktop execution remain application-owned. |
| `@braedonsaunders/appkit-agent-tools` | generalized extraction, substantially hardened | Steward `src/lib/local-tools/{runtime,schema}.ts` supplies the managed-tool model: a manifest carrying risk, capabilities, declared binaries and health-check arguments; npm-package and binary-path sources; separate install and execution approval gates; policy modes (`deny` / `approval` / `allow_safe` / `allow_all`); request-signature deduplication of pending approvals; approval TTL expiry; and scheduled health checks. AppKit does not port the source's execution path, which used `execFile` with the host process environment inherited and no isolation: execution, installation, and health checks all route through `@braedonsaunders/appkit-process-sandbox` with network isolation by default, a read-only tool directory, a single writable workdir, an explicit environment, capped output, and manifest-declared resource ceilings. Further AppKit-original hardening closes source gaps: npm installs run with `--ignore-scripts`, versions must be exact rather than ranges, approved grants carry their own expiry and use count instead of authorizing a scope permanently, grant spending is a store-level atomic operation, binary-path manifests must resolve inside allowed roots, and the singleton single-tenant store becomes a tenant-scoped port with memory and Drizzle adapters. The tool catalogue itself is application-owned. |
| `@braedonsaunders/appkit-desk` | appkit-original infrastructure | No reusable sibling microVM package existed; the sibling agent surfaces run under `@braedonsaunders/appkit-process-sandbox` bubblewrap confinement or an in-process browser driver. AppKit defines a fail-closed Cloud Hypervisor launch planner over one golden base image with per-desk copy-on-write overlays, a framed vsock guest-agent protocol with bounded frames and a closed operation set, lease-based residency with idle suspend and a bounded FIFO queue, an on-demand screen service with a pixel-space coordinate contract and opportunistic AT-SPI targeting, load-bearing handover masking, exact frame identity, a bounded content-addressed home migration contract with atomic staging, and an executable backend conformance check. Consumer-supplied policy, event-recording, and audit ports plus an injectable backend keep CI independent of a hypervisor. Tenant governance, durable session/event ledgers, approvals, autonomy dials, and operator UI remain application-owned. |
| `@braedonsaunders/appkit-egress-proxy` | appkit-original infrastructure | No reusable sibling egress package existed; the siblings' SSRF guards cover top-level navigations only, and the canonical host rules this package mirrors dependency-free live in `@braedonsaunders/appkit-sync`'s egress module. AppKit defines one fail-closed chokepoint for explicit HTTP, CONNECT, and transparently DNAT'd flows: the destination is recovered from TLS SNI or the Host header with pure parsers, a caller-supplied policy is consulted before any byte reaches a destination, allowed names resolve only to verified-public addresses, every decision is audited, and no TLS-interception code path exists. Allowlist policy, principal attribution, durable audit storage, firewall redirect rules, and operator UI remain application-owned. |
| `@braedonsaunders/appkit-avatars` | substantial generalized extraction | OpenStudio `src/types/avatar.ts`, `src/lib/avatar/{compositor,generator}.ts`, and `src/components/avatar/canvas/*` supply the category, layer, placement, prompt, and editor model. AppKit removes the product economy, Supabase/R2, and route coupling; uses one 512×768 full-body composition with a stored head viewport; renders DOM layers instead of Konva; and adds provider-injected image generation. These deliberate model and renderer changes mean the package is not presented as a drop-in source copy. |
| `@braedonsaunders/appkit-scene` | faithful generalized extraction | OpenStudio `src/components/homepage/WalkingCharacter.tsx` and the homepage scene composition supply autonomous walking, idle timing, collision-aware target selection, direction, depth scaling, and ground placement. AppKit replaces product imagery and page composition with controlled character data, optional live figure nodes, semantic tokens, and selection callbacks. |
| `@braedonsaunders/appkit-mailbox` | appkit-original infrastructure | No reusable sibling mailbox engine existed. AppKit defines a persistence-injected IMAP/SMTP boundary with UIDVALIDITY-aware incremental sync, Message-ID deduplication, RFC-5322 reply threading, and connect-time verification; tenant authorization, durable storage, credentials, scheduling, and operator UI remain application-owned. |
| `@braedonsaunders/appkit-office` | substantial generalized extraction | BeaconHS `packages/office` supplies the complete conversion runtime verbatim: `resolveSoffice` (SOFFICE_PATH override plus macOS/Linux fallbacks), `sofficeConvert`'s disposable mkdtemp work directory with a HOME-overridden profile, 180-second ceiling, and cleanup-in-finally, poppler `pdfUnite` concatenation, the shared size/MIME ceilings in `limits.ts`, and the exact-match Flat ODT text tokenizer `replaceTextInFodt` with its ported test suite. The `htmlToDocx`/`htmlToPdf`/`docxToPdf`/`docxToText`/`replaceTextInDocx` wrappers generalize the call shapes used by BeaconHS's document AI actions and render worker (`apps/web` documents `_ai-actions.ts`, `apps/worker` document-render); `renderWorkbook` generalizes the ExcelJS report-export pattern (sanitized 31-character sheet names, bold frozen header, sampled 10–56 column widths) from BeaconHS's report export route into a declarative sheet spec behind an optional `exceljs` peer. The `officeDocumentHtml` print shell and hand-rolled `sanitizeOfficeHtml` allowlist are appkit-original companions to that flow; conversion binaries (LibreOffice, poppler) remain deployment-owned. |
| `@braedonsaunders/appkit-voice` | appkit-original infrastructure | No reusable sibling voice package existed. AppKit defines provider catalogues, credential verification, per-agent cascade/realtime configuration, and LiveKit token minting with credentials injected per call; durable tenant configuration, call orchestration, and operator UI remain application-owned. Carrier number provisioning is `@braedonsaunders/appkit-telephony`. |
| `@braedonsaunders/appkit-telephony` | appkit-original infrastructure | No reusable sibling telephony package existed; `@braedonsaunders/appkit-sms` covers messaging only and `@braedonsaunders/appkit-voice` covers speech providers and LiveKit tokens. AppKit defines a carrier catalogue, sealed-secret config validation matching the `@braedonsaunders/appkit-sms` `keyCiphertext`/`keyNonce` convention, and a narrow `CarrierClient` contract over bounded `fetch` egress. `ensureTrunk` collapses a carrier's own trunk, origination, and credential objects into one normalized result — termination host/port, outbound credentials, and suggested inbound signalling ranges — and unwinds partial failures, so no consumer learns a carrier's object model. Published signalling ranges are a catalogue default to seed editable configuration, never a runtime source of truth. SIP infrastructure, media servers, ingress allowlists, number-to-agent routing, caller-id policy, durable tenant storage, and operator UI remain application-owned. |
| `@braedonsaunders/appkit-oauth` | generalized production pattern | Bunkhouse `apps/web/src/lib/mail-oauth.ts` supplies the production grammar: PKCE (S256), form-encoded token exchange and refresh with OAuth error bodies surfaced as readable messages, and hardened egress (public-HTTPS-only endpoints, DNS answers checked against private ranges, redirects refused, 15-second ceiling). AppKit generalizes provider-specific Google/Microsoft endpoints into standards-based discovery — RFC 9728 protected-resource metadata, RFC 8414/OIDC authorization-server metadata, RFC 7591 dynamic client registration as a public client, RFC 8707 resource indicators — which had no sibling implementation and follows the MCP authorization specification. Storage, state sealing, session binding, and operator UI remain application-owned. |
| `@braedonsaunders/appkit-emails` | substantial generalized extraction | The root injects secret unsealing for modular apps. Provider behavior, local loopback SMTP, delivery policy, and hardened validation are retained without application-specific entry points. |
| `@braedonsaunders/appkit-email-render` | faithful generalized extraction | Reference B `packages/email-render` copied file-for-file with all 48 upstream tests; only the package name and inline default brand are generalized |
| `@braedonsaunders/appkit-email-designer` | substantial generalized extraction | BeaconHS `apps/web` supplies the proven authoring grammar: the plain-HTML GrapesJS builder (`admin/email-templates/_builder.client.tsx`) that deliberately runs without the MJML plugin so every element stays a real editable component, its `serializeTemplateEditor` pairing of `getCss()` with `getHtml()`, the `data-each` row marker that makes a repeating table authorable at all (a `<tr>` cannot hold the text node a bare `{{#each}}` needs), the juice inliner ordered before marker expansion because cheerio would mangle handlebars, and the shared table-structure tools (`admin/_table-tools.tsx`) GrapesJS does not provide. AppKit generalizes the app-tier shell that carried it: the `@beaconhs/ui`, `sonner`, `next/*`, server-action, and i18n-codegen couplings are replaced by an overridable copy map and plain CSS keyed on `--ak-ed-*` custom properties, so the package has no UI-kit dependency; GrapesJS, React, and juice become optional peers behind `/react` and `/inline` subpaths, leaving the root entry isomorphic. Appkit-original companions: the `email`/`signature` presets and their block catalogues, the inline-baked theme with hex-only color validation, and a single `compileEmailDesign` that derives both persisted halves from the same inlined, sanitized markup — which is what makes a fragment round-trip lossless, since sanitizing a fragment drops a top-level `<style>` block. Rendering and sanitization remain `@braedonsaunders/appkit-email-render`; storage, delivery, and operator UI remain application-owned. |
| `@braedonsaunders/appkit-sms` | substantial generalized extraction | The root injects secret unsealing for modular apps. The five-provider catalogue, policy, validation, bounded responses, and credential redaction are retained without application-specific entry points. |
| `@braedonsaunders/appkit-jobs` | faithful generalized reusable queue extraction — packed consumer proven; application worker cutover pending | Reference A `packages/jobs/src/queues/{email,reports,sandbox,scripts,migration,ap-capture}.ts` plus its worker concurrency profiles, and Reference B `packages/jobs/src/queues/{email,notify,outbound,pdf,push,reports,scheduled}.ts` plus `apps/worker/src/index.ts`, are copied into factory-bound entries. The complete reusable surface is present: private email fan-out, notification batches, push, all six PDF/document variants, durable artifacts, on-demand waits, both report contracts, isolated outbound dispatch, the exact eleven-job repeat registry and stale-repeat cleanup, sync/maintenance de-duplication, non-retried authored scripts and sandboxes, migration, and deterministic capture. Queue names, bounds, IDs, retry/backoff, retention, and worker concurrency remain source-compatible; tenant/org naming is a typed compatibility union, while capture naming is injected so domain-specific strings stay in the consuming application. Redis/provider credentials and domain processors remain application-owned. Packed Node consumers exercise every public job contract; direct production worker import replacement is still pending. |
| `@braedonsaunders/appkit-storage` | faithful attachment UI extraction plus reference generalized runtime | Reference A `web/components/attachment-panel.tsx` supplies the complete two-pane upload, URL-backed search/type filter/paging, selected-file list, image/PDF preview, download, expand/restore, and removal behavior. Raw palettes are tokenized; translations, Next routing, fetch routes, authorization, and tenancy are generalized behind labels, AppKit list navigation, and an injected `AttachmentAdapter`. The source `targetTable`/`targetId` caller shape and HTTP response contract are retained. The root also provides tenant-owned keys, multipart upload, streams/ranges, rich metadata, verified promotion, lifecycle tagging, existence checks, presigning, private-bucket readiness, and strict portable environment configuration. |
| `@braedonsaunders/appkit-editor` | faithful generalized extraction | Reference B `packages/ui/src/rich-text-editor.tsx`; controlled value and host link normalization are additive generalizations, while TipTap behavior and toolbar grammar are preserved |
| `@braedonsaunders/appkit-forms-documents` | faithful package-boundary extraction | Reference B `pdf-template-html.ts` and `doc-style.ts` are retained behind the document package. The byte-identical companion contract is now canonical in forms-core and re-exported here, removing the previous duplicate implementation while keeping document consumers on the same public symbols. |
| `@braedonsaunders/appkit-forms` | faithful generalized production designer and fill-runtime extraction — reference consumer cutover pending | Reference B's complete 4,186-line production designer and 4,778-line fill renderer were copied before generalization. The designer retains its exact navigation and interaction composition: overview, complete palette, stacked/free canvas, tabs, sign-off steps, record/list/action panels, assignments, permissions, preview, immutable publishing, localized copy, and inspector surfaces. Source persistence, routing, data-source, workflow, and assistant coupling are typed host seams; the copied flow profile/linter preserves top-level write, companion, photo, and repeating-section rules. The renderer retains revision/sequence-safe autosave, unload beacons, lazy drafts, inline saves, validation, guided steps, formulas/defaults/logic, entity and data queries, repeating rows, all controls, media analysis/annotation, responsive layouts, read-only records, and submission. Static source-shaped tests, browser interaction, and package gates exercise both generalized entries; direct reference-application compile cutovers remain required before claiming completed consumer replacement. |
| `@braedonsaunders/appkit-i18n` | generalized production pattern | Tenant locale policy and localized-value resolution from both products, with application translation catalogues injected rather than bundled. |
| `@braedonsaunders/appkit-workflows` | substantial generalized extraction — subject adapters remain application-owned | Graph validation/runtime, durable gates, approval tokens, and the production multi-flow authoring composition are present: library rail, enable/rename/delete, node toolbar, compatible templates, drawer inspector, lint, save boundary, and canvas branches. Reference A's record approval actions, pending-with chip, rejection prompt, synchronized state hook, and complete approval history event list are copied into tokenized React components; its record-state query and gate-decision contracts are retained by the HTTP adapter. The provider replaces framework router/i18n coupling, while a quorum-aware memory adapter proves the same UI database-free. Subject profiles and persistence are injected; durable definition CRUD and domain execution adapters remain host-owned. |
| `@braedonsaunders/appkit-sync` | faithful generalized production orchestrator extraction — packed source-shaped consumer proven; application target cutover pending | Reference B `packages/sync/src/{orchestrator,types,registry,snapshot-policy,person-sync-policy,csv,db-drivers,egress}.ts`, all five connector families, `packages/db/src/schema/sync.ts`, and the complete upstream ownership and egress tests supply the implementation. AppKit now preserves the source `runSync({tenantId, connectionId, trigger, dryRun?})` call through `createSyncOrchestrator`, pull-before-write ordering, 250-row tenant transactions, per-record savepoints, batch rollback accounting, run logs, per-record before/after/diff/failure ledgers, source/manual ownership policy, clean-run-only cursor advancement, preview metadata isolation, conflict/partial status, explicit full-snapshot authority, and fail-closed archive transactions. The feature schema and clean initial migration retain connections, crosswalk ownership, runs, record changes, composite tenant foreign keys, indexes, and source-shaped status fields. Memory and Drizzle persistence implement the same boundary; packed consumers execute a source-shaped run and compile the complete adapter contract. The source root's crypto convenience re-export is intentionally replaced by `resolveSecrets`; its eager built-in registry and SQL-driver re-exports move to `/registry` and `/db-drivers`. These generalizations keep optional database vendors out of the server root and leave browser consumers explicit `/runtime`, `/csv`, and `/catalog` entries without removing source capabilities. The source application's canonical-table upsert implementation remains a typed target because its people, location, equipment, contact, title, catalogue, and natural-key tables are product-owned; its target adapter and worker import still need direct cutover before claiming completed application replacement. |
| `@braedonsaunders/appkit-integrations` | faithful generalized production extraction — application worker cutover pending | Reference B `packages/integrations/src/{run,dispatch,idempotency,resolve,types}.ts`, all five destination implementations, `packages/db/src/schema/integrations.ts`, the outbound builder/actions/catalogue, and the worker call sites supply the package. AppKit preserves source-shaped disabled-by-default definitions, soft deletion, the composite-tenant export ledger, deterministic per-automation queue ids, safe disabled/removed no-ops, fail-closed ledger reads, atomic ref replacement including empty clears, partial retry refs, send-once suppression only after complete delivery, identity-based reversal, best-effort status bookkeeping, hardened HTTP/chat/Sheets/email behavior, and the full SQL row/weekly fan-out, required-field, value-map, and returned-id behavior. `/react` retains the connected hub, searchable direction catalogue, declared connection/secret fields, all five mapping editors, click-to-insert tokens, test/save controls, enable/send-once policy, and secret-replacement boundary with raw palettes tokenized and framework actions injected. Product trigger catalogues/event builders, authorization, audit, secret sealing, domain outbox, and durable queue are application-owned inputs. A direct production route/worker import cutover remains pending. |
| `@braedonsaunders/appkit-customization` | faithful generalized engine, persistence, authoring, and consuming-list extraction — reference route/query cutover pending | Reference A's complete types, registry behavior, Zod schemas, parse/lint helpers, locked/versioned merge behavior, default refresh logic, and upstream tests were copied before generalization. `RecordListView`, `EntityListView`, `ViewsMenu`, `FilterChips`, sortable headers, paging, effective-view resolution, live-column merge, and the four form/list preference tables are represented by generalized package-owned contracts, React composition, memory repository, and tenant-scoped Drizzle repository. The form, list-view, and custom-field editors retain source groups, field placement, line columns, actions, filters, sorting, scope, field options/behavior, and save/delete flows. `createCustomizationEngine` moves the source catalogue to the consuming application while preserving record-type-key default/parse/lint/refresh calls; both persistence adapters validate through the same injected registry. The packed Node/React consumer renders a saved-view record table with subtabs and exercises a validated write. Currency, status meaning, routing, permission decisions, record metadata, roles, and domain queries remain injected application concerns. The full reference route and tenant query builders have not yet been cut over. |
| `@braedonsaunders/appkit-design-studio` | faithful generalized production extraction — consumer cutover compile gate pending | The Fabric editor, selection/move/resize/rotate interaction, layers and full property inspector, bounded DTO schema, factories/defaults, copy catalogue, controlled studio composition, HTML rendering, print rendering, and source behavior tests are present. Persistence, asset upload, and route policy are injected. |
| `@braedonsaunders/appkit-pdf` | faithful generalized implementation | The PDFKit/table/statement/template/Chromium engine is retained behind dependency-light root, `/template`, and `/html` entries without an application-named compatibility surface. |
| `@braedonsaunders/appkit-scheduling` | faithful generalized production extraction — host adapters remain application-owned | Reference application C's `apps/web/lib/schedule-utils.ts`, its `ScheduleTask`/`ScheduleDependency`/`ScheduleCalendar`/`ScheduleResource`/`ScheduleBaseline` domain models, and the complete `components/workspace/schedule` surface (Gantt view/bar/dependency/milestone, list, board, toolbar, filters, task editor, context menu, and schedule management) were copied before generalization. The calendar arithmetic, four dependency-type weightings, Kahn topological sort with forward/backward CPM passes, total-float and critical-set derivation, dependency-violation and cycle detection, work-breakdown depth/indent/outdent/subtree-reorder builders, duration-weighted summary rollups, timeline column/band/bounds geometry, drag-delta clamping, and the complete insight probe set are preserved behaviour-for-behaviour, with the source's own tests ported. Generalizations: product identity fields (`projectId`/`revisionId`/timestamps) are removed so hosts map their own rows; every hardcoded English string moves to an overridable `SchedulingLabels` contract with count formatters left to the host's plural rules; `en-US` date formatting becomes injectable `Intl` formatters; Tailwind palette literals become `--sched-*` token channels with light and dark values; Radix popovers become `@braedonsaunders/appkit-ui` primitives; and SVG marker ids are instance-scoped. Resource leveling (serial list scheduling by least float, within-float and unbounded modes, frozen in-flight work, unresolved-conflict reporting, and the load histogram) is appkit-original — the source detects overallocation but does not resolve it. Persistence, authorization, identity, import, and PDF export are injected host adapters. |
| `@braedonsaunders/appkit-forms-pdf` | faithful generalized production extraction | Portable summaries sit beside the production PNG rendering, bounded resource policy, browser lifecycle, report/record printers, template profiles, and source behavior tests. Chromium and storage are optional injected/runtime subpaths rather than root dependencies. |

## Forms parity boundaries

The forms packages intentionally distinguish source parity from reusable
composition:

| surface | classification | exact boundary |
|---|---|---|
| `@braedonsaunders/appkit-forms-core` form schema/evaluator/validator/scoring helpers | substantial generalized extraction — root type compatibility differs | Reference B behavior plus the reference A field/string layer and 235 core tests remain. Reference B's companion-value contract is copied into the root so forms, flows, and documents share one implementation; finance pickers add their proven human-readable companions. DOM-free text parsing and attachment URL policy stay dependency-light; rich sanitization/normalization use explicit subpaths, and document generation lives in `@braedonsaunders/appkit-forms-documents`. The generalized root widens localized strings, makes workflow metadata optional, and adds finance fields, so unmodified reference consumers must not be claimed as compile-compatible. |
| `@braedonsaunders/appkit-forms-core/safety-automation` | faithful generalized extraction | Reference B automation and flow-subject contracts, verified by the complete 11-test upstream suite |
| `@braedonsaunders/appkit-forms-core/business-automation` and `business-flow-subjects` | faithful extraction | Reference A `packages/forms-core/src/automation.ts`, `flow-subjects.ts`, and its complete 22-test automation suite; only the local import path changes |
| `LogicBuilder` | faithful generalized extraction | Reference B template-designer `logic-builder.tsx`; condition behavior and array coercion are preserved, generated i18n wrappers become injectable labels, raw colors become semantic tokens, and an additive disabled state supports read-only hosts |
| `ProductionFormRenderer` | faithful generalized production extraction | Reference B's full production renderer was copied before route, translation, upload, persistence, authorization-bound query, and analysis boundaries were converted to typed application adapters. All source runtime branches remain represented and source-shaped rendering plus packed-consumer gates prove the public entry. |
| `FormRenderer` | portable controlled AppKit composition, not production parity | The smaller controlled renderer is useful when the host owns every field value and callback. It must not be substituted for `ProductionFormRenderer` in a parity cutover. |
| `ProductionFormDesigner` | faithful generalized production extraction — reference consumer cutover pending | Reference B's full 4,186-line designer was copied before server actions, routes, data-source loading, workflows, and assistant coupling became typed host seams. All seven rail surfaces, Build/Flows workspace, field and section inspectors, stacked/free canvas, tabs, sign-off steps, preview, publishing, record controls, and locale authoring remain. Browser tests prove independent scroll containment, stable inspector geometry, the 1/3–2/3 layout, every rail panel, workflow creation, preview, and fill-runtime handoff. |
| `FormDesigner` | portable controlled AppKit composition, not production parity | The smaller controlled designer remains useful for embedded/custom hosts. It must not be substituted for `ProductionFormDesigner` in a parity cutover. |

The safety and business automation vocabularies are separate public subpaths
because identically named actions have incompatible payloads and lifecycle
semantics. A fabricated union would not be drop-in compatible with either
product. Consumers choose the source-native profile and may build an app-level
adapter to a future shared orchestration layer.

The `user_dashboard_layouts` and `insight_cards` schema in
`@braedonsaunders/appkit-dashboard/schema` follows
the two siblings' production personal/role layout and persisted insight-card
models. The playground's `members`/`roles`/`audit` semantic catalogue is demo-app
composition over real tables, not part of the reusable analytics package.

## Rule for future work

Every new primitive or package must name its production source in the commit and
in this file. If no source exists, label it **appkit-original** before building
it. “Compatible API” is not the same claim as “identical implementation,” and a
visual approximation is not an extraction.

A package cannot be called **faithful**, **complete**, or **drop-in compatible**
until all of these gates are recorded and green:

1. Inventory every public source export, subpath, schema, migration, preset, and
   runtime entry from both sibling implementations.
2. Port the source tests first, then add behavior tests for every generalized
   adapter boundary.
3. Compile representative unmodified reference-application consumer imports
   against AppKit compatibility entries.
4. For UI, compare the real source screen and AppKit screen at the same viewport
   and exercise selection, editing, scrolling, keyboard, empty, error, and dark
   states in a browser.
5. List every intentionally app-owned domain adapter. An omitted reusable
   implementation is not an adapter boundary.

Until those gates pass, use **partial extraction** and list the missing source
surface explicitly. A working demo of a subset does not establish parity.
