{
  "name": "appkit",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.30.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo run build",
    "build:packages": "turbo run build --filter='./packages/*'",
    "typecheck": "turbo run typecheck",
    "lint": "pnpm check:quality && pnpm check:public && pnpm check:boundaries && pnpm test:isolation && turbo run lint",
    "check:quality": "node scripts/check-production-quality.mjs",
    "check:public": "node scripts/check-public-surface.mjs",
    "test": "turbo run test && pnpm test:release-order && pnpm test:registry-closure",
    "test:release-order": "node --test scripts/publish-order.test.mjs",
    "test:registry-closure": "node --test scripts/registry-dependency-closure.test.mjs",
    "test:registry-consumers": "node scripts/test-registry-consumers.mjs",
    "check:boundaries": "node scripts/check-package-boundaries.mjs",
    "test:isolation": "node scripts/check-package-isolation.mjs",
    "sync:package-metadata": "node scripts/sync-package-metadata.mjs",
    "test:packages": "node scripts/test-package-tarballs.mjs",
    "test:consumers": "node scripts/test-package-consumers.mjs",
    "validate": "pnpm -r typecheck && pnpm lint && pnpm test && pnpm build && pnpm test:packages && pnpm test:consumers",
    "dev": "turbo run dev",
    "clean": "turbo run clean && rm -rf node_modules",
    "changeset": "changeset",
    "version-packages": "changeset version",
    "release": "pnpm build:packages && pnpm test:packages && pnpm test:consumers && node scripts/publish-packages-sequentially.mjs && pnpm test:registry-consumers && changeset tag"
  },
  "devDependencies": {
    "@changesets/cli": "^2.29.7",
    "turbo": "^2.5.8",
    "typescript": "^5.9.3"
  }
}
